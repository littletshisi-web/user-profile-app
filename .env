# Connection string for your local MongoDB instance.
# 'user-profile-app' is the name of the database being used.
MONGO_URI=mongodb://localhost:27017/user-profile-app

# Secret key used to sign and verify JSON Web Tokens (JWTs).
# IMPORTANT: Replace this with a strong, unpredictable value in production.
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production