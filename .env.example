# Example environment variables for local development / deployment
# MongoDB Atlas connection string with TLS/SSL enabled
# Replace <username>, <password>, and cluster details with your own
# Example for local development: mongodb://localhost:27017/user-profile-app
# Example for MongoDB Atlas (Render): mongodb+srv://user:password@cluster0.xxx.mongodb.net/user-profile-app?ssl=true&retryWrites=true&w=majority
MONGO_URI=mongodb+srv://littletshisi_db_user:<password>@cluster0.zoejifh.mongodb.net/user-profile-app?ssl=true&retryWrites=true&w=majority

# Secret key used to sign and verify JWTs (JSON Web Tokens).
# ⚠️ IMPORTANT: Replace this with a strong, unpredictable value in production.
JWT_SECRET=change_this_to_a_secure_random_value

# Node environment (development or production)
NODE_ENV=development

# Optional: override port (Render sets PORT automatically)
# PORT=3000
